export interface MeshProfile {
  loggedIn: boolean,
  showSuperAdmin?: boolean,
  showAdmin?: boolean,
  name?: string,
  description?: string,
  avatarUrl?: string,
  username?: string,
  hubUrl?: string,
  apps?: string
}
